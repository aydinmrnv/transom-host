# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Assembled app bundle + release artifacts (scripts/make-app.sh, release.sh)
/build/

# Xcode (we do not use an Xcode project, but tooling may still litter)
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/

# macOS
.DS_Store
